# Phase 0
python-dotenv>=1.0,<2.0

# Phase 1 — Face detection + embedding extraction (local, no cloud)
insightface>=0.7.3
onnxruntime>=1.16.0
Pillow>=10.0

# Phase 2 — Web / social media search (reverse image via Apify Google Lens)
requests>=2.28,<3.0

# Phase 2 fallback — headless browser for sites that block requests (LinkedIn)
playwright>=1.40.0

# Phase 4 — Blockchain upload (Sepolia testnet)
web3>=6.0.0
